{
    "name": "craft",
    "dockerComposeFile": "../docker-compose.yml",
    "workspaceFolder": "/app",
    "service": "web",
    "shutdownAction": "stopCompose",
    "extensions": [],
    "settings": {},
    "forwardPorts": [80],
    "postCreateCommand": "cp .env.example.dev .env && mv composer.json.default composer.json && composer install && bash -i -c 'php craft install'",
    "portsAttributes": {
        "80": {
            "label": "HTTP"
        }
    }
}
