{
    "name": "craft",
    "dockerComposeFile": "../docker-compose.yml",
    "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind,consistency=cached",
    "workspaceFolder": "/app",
    "service": "web",
    "shutdownAction": "stopCompose",
    "extensions": [],
    "settings": {},
    "forwardPorts": [8080],
    "initializeCommand": "cp -n .env.example.dev .env && [[ -f composer.json.default ]] && mv composer.json.default composer.json",
    "postCreateCommand": "composer install",
    "portsAttributes": {
        "8080": {
            "label": "HTTP"
        }
    }
}
