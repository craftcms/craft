{
    "name": "craft",
    "dockerComposeFile": "../docker-compose.yml",
    "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind,consistency=cached",
    "workspaceFolder": "/app",
    "service": "workspace",
    "shutdownAction": "stopCompose",
    "extensions": [],
    "settings": {},
    "forwardPorts": [8080],
    "initializeCommand": "cp -n .env.example.dev .env ; test -f composer.json.default && test ! -f composer.json && mv composer.json.default composer.json ; echo \"Configuration files are in place!\"",
    "postCreateCommand": "composer install && php craft install",
    "portsAttributes": {
        "8080": {
            "label": "HTTP"
        }
    }
}
